name: Create Release and Update Repository

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    uses: Submaruiner/DalamudPluginRepo/.github/workflows/reusable_create_release.yml@main
    with:
      internal_name: AchManager
      solution_name: AchManager
      build_configuration: Release
      personal_plugin_repo: Submaruiner/DalamudPluginRepo
      personal_plugin_repo_branch: main
    secrets: inherit
